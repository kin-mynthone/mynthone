import 'package:flutter/material.dart';

class SlashedEyeIcon extends StatelessWidget {
  const SlashedEyeIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.visibility_off,
      color: Colors.grey,
      size: 30,
    );
  }
}
